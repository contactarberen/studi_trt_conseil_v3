<form method="post" action="{{ path('app_attributs_recruteur_delete', {'id': attributs_recruteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attributs_recruteur.id) }}">
    <button class="btn">Delete</button>
</form>
